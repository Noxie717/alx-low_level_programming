#include "main.h"
/**
 * rev_string - reverses a string
 * @s: char type
 */
void rev_string(char *s)
{
	int b, d;
	char g;

	for (b = 0; s[b] != '\0'; b++)
		;

	for (b--, d = 0; d < 1 / 2; b--, d++)
	{
		g = s[d];
		s[d] = s[b];
		s[b] = g;
	}
}
