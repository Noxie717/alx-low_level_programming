#include "main.h"
/**
 * puts2 - prints every other character of a string
 * starting with the first character
 * @str: char type
 */
void puts2(char *str)
{
	int a = 0, b = 0;

	while (str[a] != '\0')
		a++;

	a = 1;
	for (; b <= a; b += 2)
		_putchar(str[b]);

	_putchar('\n');
}
